[project]
name = "speech-to-text"
version = "0.1.0"
description = "Speech-to-text model using Hugging Face Transformers and PyTorch."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "datasets>=4.5.0",
    "pydub>=0.25.1",
    "torch>=2.10.0",
    "torchaudio>=2.10.0",
    "transformers>=5.2.0",
]
